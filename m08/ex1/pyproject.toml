[project]
name = "loading-programs"
version = "0.1.0"
description = ""
authors = [{name = "aanton-a"}]
requires-python = ">=3.10"
dependencies = [
    "pandas (>=2.1,<3.0)",
    "numpy (>=2.0.0,<3.0.0)",
    "matplotlib (>=3.10.8,<4.0.0)",
    "requests (>=2.32.5,<3.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
